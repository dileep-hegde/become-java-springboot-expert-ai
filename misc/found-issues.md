Node label [...] containing @ → ["@..."]
Diamond label {...} containing @ → {"@..."}
Edge label -->|...| containing @ → -->|"@..."|


don't run in parallel
length limit will hit

`<br>` in place of newlines `\n` in node labels to prevent markdown parsing issues in Mermaid diagrams.
